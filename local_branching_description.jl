#local_branching_description.jl
#=
    It looks like when I create a new branch (test_branch) in Atom, it
    changes my local files to just the branch files, not the main files.
    This means I "lose" the main files locally, but they are all online?
=#